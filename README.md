# 🖥️ Python Remote Desktop (LAN)

A simple remote desktop system built using Python sockets that streams the host screen to a client over a local network.

---

## 🚀 Features
- Real-time screen streaming
- Works over LAN (same WiFi)
- Lightweight implementation

---

## 📦 Requirements
```bash
pip install opencv-python numpy pyautogui pillow
